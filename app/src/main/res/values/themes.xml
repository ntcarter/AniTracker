<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AniTracker" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/background_black</item>
        <item name="colorPrimaryVariant">@color/background_black</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="AniTracker.MaterialComponents.Chip.Choice" parent="Widget.MaterialComponents.Chip.Choice">
        <!-- background color -->
        <item name="chipBackgroundColor">@color/bg_chip_state_list</item>
        <item name="chipStrokeColor">@color/text_red</item>
        <item name="chipStrokeWidth">2dp</item>
        <item name="android:textStyle"></item>
        <item name="android:textColor">@color/text_chip_state_list</item>
    </style>

    <style name="Theme.AniTracker.cardView" parent="Widget.MaterialComponents.CardView">
        <item name="cardBackgroundColor">@color/background_black</item>
        <item name="strokeWidth">2dp</item>
        <item name="strokeColor">@color/text_red</item>
    </style>

    <style name="AlertDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="buttonBarPositiveButtonStyle">@style/PositiveButtonStyle</item>
        <item name="android:background">@color/background_black</item>
        <item name="android:textColorPrimary">@color/text_red</item>
    </style>

    <style name="PositiveButtonStyle" parent="Widget.MaterialComponents.Button.TextButton.Dialog">
        <item name="android:textColor">@color/text_red</item>
    </style>
</resources>